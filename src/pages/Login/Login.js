import React from 'react';
import styled from 'styled-components';

const Login = () => {
  return <LoginPage>로그인입니다~</LoginPage>;
};

export default Login;

const LoginPage = styled.h1`
  color: purple;
  font-size: 24px;
`;
